function App(): React.JSX.Element {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
        backgroundColor: '#000',
        color: '#fff',
        fontFamily: 'sans-serif',
        fontSize: '2rem'
      }}
    >
      Open Photobooth — Loading...
    </div>
  )
}

export default App
